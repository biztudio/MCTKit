<template>
<wxc-tab 
    bind:tabchange="onClick"
    default-index="{{0}}"
    active-text-color="#108ee9"
    active-line-color="#108ee9"
    component-id="c1"
    animate="{{true}}">
      <wxc-tab-panel
        wx:for="{{tabs}}"
        wx:for-item="tab"
        wx:key="{{tab.content}}"
        tab-index="{{index}}"
        component-id="c1"
        label="{{tab.title}}">

            <block wx:if="{{index > 0}}">
                 {{tab.content}}
            </block>
            <block wx:elif="{{index == 0}}">
                <schultegrid ></schultegrid>
            </block>

      </wxc-tab-panel>
  </wxc-tab>
</template>
<script>

export default {
 
 methods:{
    onClick:function(event){
      console.log('ComponentID: ' + event.detail.componentId + ', you selected ' + event.detail.key)
    }
  },

 config: {
    usingComponents: {
      'wxc-tab': '@minui/wxc-tab',
      'wxc-tab-panel': '@minui/wxc-tab/panel',
      'schultegrid': 'layout/schultegrid'      
    }
  },

  data:{
    tabs:[
      {title:'舒尔特方格', content:'list'},
      //{title:'训练指南', content:'content 2'},
      {title:'设置', content:'content 3'}
    ]
  }
}
</script>

<style>

</style>
